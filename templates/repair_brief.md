# Repair after forge run {{job_id}}

The forge run just finished, and `./guy boot-test` fails. Fix it so the widget boots and every page renders. Keep as much of the new work as you can. Run `./guy boot-test` yourself until it exits 0.

```
{{error}}
```
